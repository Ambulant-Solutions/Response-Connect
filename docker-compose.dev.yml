services:
  web:
    environment:
      FLASK_APP: wsgi:app
      FLASK_DEBUG: "1"
    command: sh -c "alembic upgrade head && flask --app wsgi:app run --host 0.0.0.0 --port 8000 --debug"
    volumes:
      - .:/app
